"use client";

import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { BarChart3, CheckCircle2, Filter, Gauge, MessageCircle, RefreshCw, Share2, Sparkles, TrendingUp, Users } from "lucide-react";
import { scorePostContent } from "@/lib/analytics/actions";
import type { AnalyticsDashboardData } from "@/lib/analytics/types";
import type { Platform, Post } from "@/types/database";

const platformLabels: Record<Platform, string> = { INSTAGRAM: "Instagram", FACEBOOK: "Facebook", LINKEDIN: "LinkedIn", X: "X" };
const platformShort: Record<Platform, string> = { INSTAGRAM: "IG", FACEBOOK: "FB", LINKEDIN: "IN", X: "X" };
const platformOrder: Platform[] = ["INSTAGRAM", "FACEBOOK", "LINKEDIN", "X"];

function formatNumber(value: number) { return new Intl.NumberFormat("en-US", { notation: value >= 10000 ? "compact" : "standard", maximumFractionDigits: 1 }).format(value); }
function formatPercent(value: number) { return `${value.toFixed(2)}%`; }
function MetricCard({ label, value, icon: Icon, helper }: { label: string; value: string; icon: typeof TrendingUp; helper?: string }) { return <div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-slate-100 text-slate-600"><Icon size={18} /></div><span className="text-xs font-medium text-slate-400">MVP</span></div><div className="mt-4 text-2xl font-bold tracking-tight text-slate-950">{value}</div><div className="mt-1 text-sm font-medium text-slate-600">{label}</div>{helper && <div className="mt-1 text-xs text-slate-400">{helper}</div>}</div>; }

function LineChart({ points }: { points: AnalyticsDashboardData["engagementOverTime"] }) {
  if (!points.length) return <EmptyChart message="Publish posts to see engagement over time." />;
  const width = 700, height = 250, pad = 32;
  const max = Math.max(...points.map((p) => p.engagementRate), 1);
  const coords = points.map((point, index) => { const x = pad + (index / Math.max(points.length - 1, 1)) * (width - pad * 2); const y = height - pad - (point.engagementRate / max) * (height - pad * 2); return { x, y, point }; });
  const polyline = coords.map((p) => `${p.x},${p.y}`).join(" ");
  return <div className="h-72 w-full overflow-hidden rounded-lg bg-slate-50/70 p-3"><svg viewBox={`0 0 ${width} ${height}`} className="h-full w-full" role="img" aria-label="Engagement rate over time"><line x1={pad} y1={height-pad} x2={width-pad} y2={height-pad} stroke="currentColor" className="text-slate-200" /><line x1={pad} y1={pad} x2={width-pad} y2={pad} stroke="currentColor" className="text-slate-200" strokeDasharray="4 5" /><line x1={pad} y1={height/2} x2={width-pad} y2={height/2} stroke="currentColor" className="text-slate-200" strokeDasharray="4 5" /><polyline points={polyline} fill="none" stroke="currentColor" strokeWidth="3" className="text-blue-600" strokeLinejoin="round" strokeLinecap="round" />{coords.map(({ x, y, point }) => <circle key={point.date} cx={x} cy={y} r="4" fill="currentColor" className="text-blue-600"><title>{point.date}: {formatPercent(point.engagementRate)}</title></circle>)}</svg><div className="flex justify-between px-2 text-[11px] text-slate-400"><span>{points[0].date}</span><span>{points[Math.floor(points.length / 2)]?.date}</span><span>{points[points.length - 1].date}</span></div></div>;
}

function EmptyChart({ message }: { message: string }) { return <div className="flex h-72 items-center justify-center rounded-lg border border-dashed border-slate-200 bg-slate-50/50 px-6 text-center text-sm text-slate-500">{message}</div>; }

function HorizontalBars({ rows, label, value }: { rows: Array<{ name: string; value: number; helper: string }>; label: string; value: (v: number) => string }) {
  if (!rows.length) return <EmptyChart message={`No ${label.toLowerCase()} data yet.`} />;
  const max = Math.max(...rows.map((row) => row.value), 1);
  return <div className="space-y-5">{rows.map((row) => <div key={row.name}><div className="mb-2 flex items-center justify-between gap-4"><div className="min-w-0"><div className="truncate text-sm font-semibold text-slate-800">{row.name}</div><div className="text-xs text-slate-400">{row.helper}</div></div><div className="text-sm font-semibold text-slate-700">{value(row.value)}</div></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{ width: `${Math.max(3, (row.value / max) * 100)}%` }} /></div></div>)}</div>;
}

function Filters({ platform, setPlatform, category, setCategory, categories }: { platform: Platform | "ALL"; setPlatform: (v: Platform | "ALL") => void; category: string; setCategory: (v: string) => void; categories: Array<{ id: string; name: string }> }) { return <div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-slate-400"><Filter size={14} /> Filters</div><select value={platform} onChange={(e) => setPlatform(e.target.value as Platform | "ALL")} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 outline-none focus:border-blue-500"><option value="ALL">All platforms</option>{platformOrder.map((p) => <option key={p} value={p}>{platformLabels[p]}</option>)}</select><select value={category} onChange={(e) => setCategory(e.target.value)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 outline-none focus:border-blue-500"><option value="ALL">All categories</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div>; }

export function AnalyticsDashboard({ initialData, categories, posts, initialPlatform = "ALL", initialCategory = "ALL" }: { initialData: AnalyticsDashboardData; categories: Array<{ id: string; name: string }>; posts: Post[]; initialPlatform?: Platform | "ALL"; initialCategory?: string }) {
  const [platform, setPlatform] = useState<Platform | "ALL">(initialPlatform);
  const [category, setCategory] = useState(initialCategory);
  const router = useRouter();
  const [scorePostId, setScorePostId] = useState(posts[0]?.id || "");
  const [audience, setAudience] = useState("Young professionals and customers interested in modern brands");
  const [scoreResult, setScoreResult] = useState<{ score: number; breakdown: Record<string, number>; recommendations: string[]; provider: string } | null>(null);
  const [scoreError, setScoreError] = useState("");
  const [isPending, startTransition] = useTransition();

  const data = initialData;

  function updateFilter(nextPlatform: Platform | "ALL", nextCategory: string) {
    const params = new URLSearchParams();
    if (nextPlatform !== "ALL") params.set("platform", nextPlatform);
    if (nextCategory !== "ALL") params.set("category", nextCategory);
    router.replace(`/analytics${params.toString() ? `?${params.toString()}` : ""}`);
  }


  function handleScore() {
    if (!scorePostId) { setScoreError("Create or publish a post before scoring content."); return; }
    setScoreError(""); setScoreResult(null);
    const formData = new FormData(); formData.set("post_id", scorePostId); formData.set("target_audience", audience);
    startTransition(async () => { const result = await scorePostContent(formData); if (!result.ok) setScoreError(result.error); else setScoreResult({ score: result.score.score, breakdown: result.score.breakdown, recommendations: result.score.recommendations, provider: result.provider }); });
  }

  const scorePost = posts.find((p) => p.id === scorePostId);
  return <div className="mx-auto max-w-7xl space-y-6">
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-blue-600">Performance intelligence</p><h1 className="mt-1 text-2xl font-bold tracking-tight text-slate-950 sm:text-3xl">Analytics</h1><p className="mt-1 text-sm text-slate-500">Understand what is resonating and improve your next piece of content.</p></div><div className="inline-flex items-center gap-2 self-start rounded-full border border-slate-200 bg-white px-3 py-1.5 text-xs font-semibold text-slate-600 sm:self-auto"><BarChart3 size={14} /> Demo analytics fallback enabled</div></div>

    <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><Filters platform={platform} setPlatform={(value) => { setPlatform(value); updateFilter(value, category); }} category={category} setCategory={(value) => { setCategory(value); updateFilter(platform, value); }} categories={categories} /></section>

    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Published Posts" value={formatNumber(initialData.summary.totalPublishedPosts)} icon={CheckCircle2} /><MetricCard label="Likes" value={formatNumber(initialData.summary.likes)} icon={TrendingUp} /><MetricCard label="Comments" value={formatNumber(initialData.summary.comments)} icon={MessageCircle} /><MetricCard label="Shares" value={formatNumber(initialData.summary.shares)} icon={Share2} /></div>
    <div className="grid gap-4 sm:grid-cols-3"><MetricCard label="Reach" value={formatNumber(initialData.summary.reach)} icon={Users} /><MetricCard label="Impressions" value={formatNumber(initialData.summary.impressions)} icon={BarChart3} /><MetricCard label="Engagement Rate" value={formatPercent(initialData.summary.engagementRate)} icon={Gauge} helper="Interactions ÷ reach" /></div>

    <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-semibold text-slate-950">Engagement Over Time</h2><p className="mt-1 text-xs text-slate-500">Engagement rate across published content.</p></div><span className="rounded-md bg-slate-100 px-2 py-1 text-xs font-medium text-slate-500">{data.bestPosts.length ? "Filtered view" : "No matching posts"}</span></div><LineChart points={data.engagementOverTime} /></section>

    <div className="grid gap-6 xl:grid-cols-2"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-5"><h2 className="font-semibold text-slate-950">Platform Performance</h2><p className="mt-1 text-xs text-slate-500">Compare engagement by platform.</p></div><HorizontalBars label="platform" rows={data.platformPerformance.map((row) => ({ name: platformLabels[row.platform], value: row.engagementRate, helper: `${row.posts} published · ${formatNumber(row.reach)} reach` }))} value={formatPercent} /></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-5"><h2 className="font-semibold text-slate-950">Category Performance</h2><p className="mt-1 text-xs text-slate-500">See which content themes perform best.</p></div><HorizontalBars label="category" rows={data.categoryPerformance.map((row) => ({ name: row.categoryName, value: row.engagementRate, helper: `${row.posts} published · ${formatNumber(row.reach)} reach` }))} value={formatPercent} /></section></div>

    <section className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 className="font-semibold text-slate-950">Best Performing Content</h2><p className="mt-1 text-xs text-slate-500">Top published posts ranked by engagement rate.</p></div><TrendingUp size={18} className="text-slate-400" /></div>{data.bestPosts.length === 0 ? <div className="px-5 py-12 text-center text-sm text-slate-500">No published posts match these filters.</div> : <div className="divide-y divide-slate-100">{data.bestPosts.map((post, index) => <div key={post.id} className="grid gap-4 px-5 py-4 md:grid-cols-[32px_1fr_110px_120px] md:items-center"><div className="flex h-7 w-7 items-center justify-center rounded-full bg-slate-100 text-xs font-bold text-slate-500">{index + 1}</div><div className="min-w-0"><div className="flex items-center gap-2"><span className="text-xs font-semibold text-slate-500">{platformShort[post.platform]}</span><span className="text-xs text-slate-400">· {post.categoryName}</span></div><div className="mt-1 truncate font-medium text-slate-900">{post.title}</div><div className="mt-1 truncate text-xs text-slate-500">{post.caption || "No caption"}</div></div><div><div className="text-sm font-bold text-slate-900">{formatPercent(post.engagementRate)}</div><div className="text-xs text-slate-400">engagement</div></div><div className="text-xs text-slate-500">{formatNumber(post.likes)} likes<br />{formatNumber(post.reach)} reach</div></div>)}</div>}</section>

    <section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-start"><div><div className="flex items-center gap-2"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-slate-100 text-slate-600"><Sparkles size={18} /></div><div><h2 className="font-semibold text-slate-950">Content Intelligence</h2><p className="mt-1 text-xs text-slate-500">Score a post from 0–100 using your existing AI provider, with a deterministic fallback.</p></div></div></div>{scoreResult && <div className="flex items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-3 py-1.5 text-xs font-semibold text-slate-600">{scoreResult.provider.startsWith("mock") ? "Demo Mode" : `Real API · ${scoreResult.provider}`}</div>}</div><div className="mt-5 grid gap-5 lg:grid-cols-[320px_1fr]"><div className="space-y-3"><label className="block text-xs font-semibold text-slate-600">Post<select value={scorePostId} onChange={(e) => setScorePostId(e.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-700 outline-none focus:border-blue-500"><option value="">Choose a post</option>{posts.map((post) => <option key={post.id} value={post.id}>{post.title}</option>)}</select></label><label className="block text-xs font-semibold text-slate-600">Target audience<input value={audience} onChange={(e) => setAudience(e.target.value)} maxLength={300} className="mt-1.5 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm text-slate-700 outline-none focus:border-blue-500" /></label><button type="button" onClick={handleScore} disabled={isPending || !scorePostId} className="inline-flex w-full items-center justify-center gap-2 rounded-lg bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-50">{isPending ? <><RefreshCw size={15} className="animate-spin" /> Scoring…</> : <><Sparkles size={15} /> Score Content</>}</button>{scoreError && <p className="text-xs font-medium text-red-600">{scoreError}</p>}</div>{!scoreResult ? <div className="flex min-h-56 items-center justify-center rounded-lg border border-dashed border-slate-200 bg-slate-50/50 px-6 text-center"><div><Gauge size={24} className="mx-auto text-slate-400" /><p className="mt-2 text-sm font-semibold text-slate-700">No score yet</p><p className="mt-1 text-xs text-slate-500">Select a post and score it to see strengths and recommendations.</p></div></div> : <div className="grid gap-5 md:grid-cols-[160px_1fr]"><div className="flex flex-col items-center justify-center rounded-xl bg-slate-50 p-5"><div className="text-5xl font-bold tracking-tight text-slate-950">{scoreResult.score}</div><div className="mt-1 text-sm font-medium text-slate-500">/ 100</div><div className="mt-3 text-xs font-semibold text-blue-600">Content Score</div></div><div><div className="grid gap-3 sm:grid-cols-2"><ScoreBar label="Hook strength" value={scoreResult.breakdown.hookStrength} /><ScoreBar label="Readability" value={scoreResult.breakdown.readability} /><ScoreBar label="CTA strength" value={scoreResult.breakdown.ctaStrength} /><ScoreBar label="Platform fit" value={scoreResult.breakdown.platformSuitability} /><ScoreBar label="Audience relevance" value={scoreResult.breakdown.audienceRelevance} /><ScoreBar label="Hashtag quality" value={scoreResult.breakdown.hashtagQuality} /></div><div className="mt-5 rounded-lg border border-slate-200 bg-white p-4"><div className="text-xs font-semibold uppercase tracking-wide text-slate-400">Recommendations</div><ul className="mt-2 space-y-2">{scoreResult.recommendations.map((recommendation, i) => <li key={`${recommendation}-${i}`} className="flex gap-2 text-sm text-slate-600"><span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-blue-600" />{recommendation}</li>)}</ul></div></div></div>}</div>{scorePost && <p className="mt-4 text-xs text-slate-400">Scored: {scorePost.title}</p>}</section>
  </div>;
}

function ScoreBar({ label, value }: { label: string; value: number }) { return <div><div className="mb-1 flex justify-between text-xs font-medium text-slate-500"><span>{label}</span><span>{Math.round(value)}</span></div><div className="h-1.5 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{ width: `${Math.max(2, Math.min(100, value))}%` }} /></div></div>; }
